// DEPRECATED — Reports has been merged into Dashboard.
// The /reports route now redirects to /dashboard. This file is kept as
// an empty stub so removing it doesn't require deleting through the IDE.
// It is not imported anywhere in the app.
export default function Reports() {
  return null;
}
